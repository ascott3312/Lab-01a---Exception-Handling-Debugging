using System;

namespace NumbersGame
{
    class Program
    {
        static void Main(string[] args) static void StartSequence()
        static void Populate(int[1])
        static void GetSum(int[])
        static void GetProduct(int[])
        static void GetQuotient(decimal, int[])
        int sum = 0;
        int product = 0;
        int quotient = 0;
        {
            for (i = 0; i < args.Lenght; i++);
            Console.WriteLine(“Enter a number greater than zero”.);
            Console.Read.Convert.ToInt32();
            Return Populate([i]);
  
            for (i = 0; i < args.Lenght; i++);
             if sum = x + y < 20 {
             Console.WriteLine“Value of { sum } is too low”;
             Console.Read.Covert.ToInt32();
             Return GetSum();

            for (i = 0; i<args.Lenght; i++);
             if quotient % 2 {
             Console.WriteLine“Value of { sum } is too low”;
             Console.Read.Covert.ToInt32();
             Return GetSum();
    }
    }
    Populate();
    GetSum();
    GetProduct();
    GetQuotient();
}


}

